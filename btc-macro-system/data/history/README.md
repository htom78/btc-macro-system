# History Store

`observations.jsonl` is generated by `python3 btc-macro-system/run.py`.

Each line is one market-date observation. The updater rewrites the file with one row per BTC market date, so repeated runs on the same day replace the prior row instead of duplicating it.

The JSONL file is ignored by git because it is local long-run data.
